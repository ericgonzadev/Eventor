<%= form_for(current_user.active_attends.build, remote: true) do |f| %>
  <%= hidden_field_tag :attended_event_id, @event.id %>
  <%= f.submit "I'm Going", class: "btn btn-primary btn-xl" %>
<% end %>
